cwe78="""whitelisting of inputs, restricting inputs to only safe inputs with regex, using an arugment list instead of shell=True in the method subprocess.run"""
cwe79="""context-aware output encoding/escaping, using template engines with autoescape enabled, sanitizing/allowlisting HTML (e.g., bleach)"""
cwe89="""parameterized queries"""
cwe94="""strict allowlisting/parsing instead of eval/exec, using ast.literal_eval or json.loads for data, executing only in a restricted/sandboxed environment (e.g., RestrictedPython)"""
